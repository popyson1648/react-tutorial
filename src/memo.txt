コンポーネント内のステート変数は任意のprops名で渡せる。

---

- historyステート変数は、ボードの状態を保持する。（マスx9の配列の集合）

最初のボードはuseStateで宣言されて、それがBoardに渡される
Board内では、その配列をコピーして、コピー後の新たな配列に変更（OかXを書き込み）を加える
変更後、配列をonPlay関数に渡す
渡された配列はGame内のhandlePlay関数内で、historyステート変数に追加書き込みされる


historyの二次元配列の要素（配列）は、最小値4、最大値10。

